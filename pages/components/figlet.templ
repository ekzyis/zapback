package components

import "github.com/ekzyis/zapback/lib"

templ Figlet(font string, text string) {
	<code class="figlet">
		<strong>
			<pre>
				{ lib.Figlet(font, text) }
			</pre>
		</strong>
	</code>
}
